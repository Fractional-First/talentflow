

## Add "Now we need your details" Title

Add a section title "Now we need your details" above the Contracting Type section in the Agreement page (`src/pages/dashboard/Agreement.tsx`), placed between the Contractual Roadmap and the Contracting Type Section.

### Technical Details

**File: `src/pages/dashboard/Agreement.tsx`**
- Insert an `h2` heading with text "Now we need your details" between the `<ContractualRoadmap />` component and the `<ContractingTypeSection />` component
- Style it consistently with the page design: `text-xl font-semibold text-foreground`

